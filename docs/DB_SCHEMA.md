# Apex Solutions Database Schema

M0 creates only `schema_migrations`. The core domain schema from the project plan should be added as M1 migrations after the relationships, indexes, privacy rules, and deletion behavior are reviewed.

Engine: InnoDB. Charset: utf8mb4.
